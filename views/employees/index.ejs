<!DOCTYPE html>
<html>
  <%- include("../static/layout.ejs") %>
  <h1 class="text-center">Funcionários</h1>
  <div class="tableMargins">
    <button type="button" class="btn btn-secondary float-end" onclick="location.href=window.location.href+'/create'">Criar Funcionário</button>
    <table class="table table-dark table-striped table-hover">
      <thead>
      <tr>
        <th scope="col">Nome</th>
        <th scope="col">Email</th>
        <th scope="col">Detalhes</th>
        <th scope="col">Editar</th>
        <th scope="col">Apagar</th>
      </tr>
    </thead>
    <tbody>
      <%if(employees.length > 0){%>
        <% for(let employee of employees) { %>
          <tr>
            <td scope="row"><%= employee.name %></th>
            <td><%= employee.email %></td>
            <td><i class="fa-solid fa-circle-plus icons" onclick="location.href=window.location.href+'/details/<%= employee._id %>'"></i></td>
            <td><i class="fa-solid fa-pen icons"></i></td>
            <td><i class="fa-solid fa-trash-can icons"></i></td>
          </tr>
        <% } %>
      <% } else { %>
          <tr>
            <td scope="row" colspan="5" class="text-center">Não foram encontrados dados!</td>
          </tr>
        <% } %>
    </tbody>
  </table>
</div>
<%- include("../static/footer.ejs") %>
</html>
